package sixthpackage;

public enum Days {

    Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday

}

// create an enum with the months of the year
// and show the usage of it based on different values from the enum